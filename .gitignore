# macOS
.DS_Store
.DS_Store?
._*

# IDE / редакторы
.idea/
.vscode/
*.swp
*.swo
*~

# Claude Code: локальные пользовательские разрешения (не шарить)
.claude/settings.local.json

# Прочие учебные материалы (16 ГБ видео — не для git)
Полиглот. 16 уроков испанского языка. 2015/

# Временные файлы
*.tmp
*.log

# PDF-источник (~128 МБ): по умолчанию исключён из репозитория, потому
# что GitHub отбрасывает файлы > 100 МБ без Git LFS. Чтобы добавить
# его в историю — см. GIT-WORKFLOW.md → раздел «PDF и Git LFS».
gramatica del uso del espanol.pdf
